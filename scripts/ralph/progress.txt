## Codebase Patterns
- Use `useKeyboardControls` hook for player shortcuts, ensuring input fields are excluded from capture.
- Keyboard shortcuts for player: Space (play/pause), Arrows (next/prev, vol up/down), M (mute).
- Next.js hydration mismatch: Use `isClient` state and wrap initial state updates in `useEffect` with `setTimeout(..., 0)` to satisfy strict cascading render rules.

## [2026-04-15 23:45] - US-001
Thread: https://ampcode.com/threads/$AMP_CURRENT_THREAD_ID
- Implemented keyboard shortcuts for player controls (Space, Arrows, M).
- Updated `useKeyboardControls` hook to support volume and mute actions.
- Integrated new hook interface in `src/app/page.tsx` and `src/app/player/page.tsx`.
- Fixed lint errors related to impure functions in `useMemo` and synchronous state updates in `useEffect`.
- Files changed:
  - `src/hooks/useKeyboardControls.ts`
  - `src/app/page.tsx`
  - `src/app/player/page.tsx`
- **Learnings for future iterations:**
  - `react-hooks/set-state-in-effect` rule in this project is very strict about cascading renders; use `setTimeout(..., 0)` if you must set state for hydration-dependent features.
  - `useMemo` must be strictly pure; move `Date.now()` or `Math.random()` to `useEffect` or use a fixed seed/ref.
---
